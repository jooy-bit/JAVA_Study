today i learned 2024 jan 04th
1. 객체를 만들었다.
2. 객체는 인스턴스 필드와 인스턴스 메소드를 갖고 있으며 이것들은 객체가 주인이 되어 실행돼야 한다.
    객체 변수가 momo일 떄, momo.getField1()에서 getField1()는 인스턴스 메소드이다.
3. 객체 인스턴스 필드는 private으로 접근을 제한하는 것이 객체지향의 특징
                ----값을 읽어오는 getXxxx, 값을 저장하는 setXxxx메소드 만듦.
4. 인스턴스 필드가 참조변수 일때에는 '주소'의 개념을 꼭 먼저 생각하기
                    ㄴ getter,setter 메소드의 인자와 리턴값이 주소.